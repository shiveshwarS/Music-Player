import 'package:flutter/material.dart';

class PlaylistProvider extends ChangeNotifier {
  // Will manage playlist CRUD and favourites in a later phase.
  // Currently delegated to Hive directly in existing code.
}
